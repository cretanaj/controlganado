<%- include('partials/_header') %>
<%- include('partials/_menu') %>
<div class="main">
    <%- include('partials/_menuheader') %>
    <main class="content">    
        <div class="container-fluid">
            <div class="header">
                <h1 class="header-title">
                    Tipo Notificacion
                </h1>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/">Inicio</a></li>
                    </ol>
                </nav>
            </div>
            
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title">Tipo Notificacion</h5>
                            <h6 class="card-subtitle text-muted">Catalogo de clases visuales para notificaciones.</h6>
                        </div>
                        <div class="card-body">
                            <% const canManageTipoNotificaciones = can('tipo_notificaciones', 'editar') || can('tipo_notificaciones', 'eliminar'); %>
                            <table id="datatables-reponsive" class="table table-striped" style="width:100%">
                                <thead>
                                    <tr>
                                        <th>N°</th>
                                        <th>Codigo</th>
                                        <th>Notificacion Class</th>
                                        <% if (canManageTipoNotificaciones) { %>
                                        <th>Acciones</th>
                                        <% } %>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (data) { %>
                                        <% for (var i = 0; i < data.length; i++) { %>
                                            <tr>
                                                <td><%= (i + 1) %></td>
                                                <td><%= data[i].codigo %></td>
                                                <td><%= data[i].notificacionClass %></td>
                                                <% if (canManageTipoNotificaciones) { %>
                                                <td class="table-action">
                                                    <% if (can('tipo_notificaciones', 'editar')) { %>
                                                    <a href="/tipo_notificaciones/updateTipoNotificacion/<%= data[i].idTipoNotificacion %>"><i class="align-middle fas fa-fw fa-pen"></i></a>
                                                    <% } %>
                                                    <% if (can('tipo_notificaciones', 'eliminar')) { %>
                                                    <a href="/tipo_notificaciones/deleteTipoNotificacion/<%= data[i].idTipoNotificacion %>"><i class="align-middle fas fa-fw fa-trash"></i></a>
                                                    <% } %>
                                                </td>
                                                <% } %>
                                            </tr>
                                        <% } %>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div> 
            
            <% if (can('tipo_notificaciones', 'crear')) { %>
            <div class="row">
                <div class="col-12 col-xl-6">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title">Agregar un nuevo registro</h5>
                            <h6 class="card-subtitle text-muted">Ingrese la informacion solicitada a continuacion.</h6>
                        </div>
                        <div class="card-body">
                            <form action="/tipo_notificaciones/addTipoNotificacion" method="post">
                                <div class="mb-3">
                                    <label class="form-label">Codigo</label>
                                    <input type="text" class="form-control" name="codigo" placeholder="Codigo">
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Notificacion Class</label>
                                    <input type="text" class="form-control" name="notificacionClass" placeholder="ms-1 text-danger fas fa-fw fa-bell">
                                </div>
                                <button type="submit" class="btn btn-primary">Salvar</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>

        </div>
    </main>
<%- include('partials/_footer') %>